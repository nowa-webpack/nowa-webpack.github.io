{
  "name": "nowa-webpack.github.io",
  "scripts": {
    "start": "gitbook serve _docs docs",
    "save": "gitbook build _docs docs && git add . --all && git commit -am ci && git push origin master"
  }
}
